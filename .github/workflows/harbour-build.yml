name: Harbour Build

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        component: ["api", "web"]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Log in to Azure Container Registry
        run: az acr login --name ${{ secrets.ACR_NAME }}
      - name: Determine Dockerfile path
        id: dockerfile
        run: |
          if [ -f "./${{ matrix.component }}/Dockerfile" ]; then
            echo "path=./${{ matrix.component }}/Dockerfile" >> $GITHUB_OUTPUT
            echo "context=./${{ matrix.component }}" >> $GITHUB_OUTPUT
          elif [ -f "./Dockerfile.${{ matrix.component }}" ]; then
            echo "path=./Dockerfile.${{ matrix.component }}" >> $GITHUB_OUTPUT
            echo "context=." >> $GITHUB_OUTPUT
          else
            echo "ERROR: No Dockerfile found for ${{ matrix.component }}" >&2
            exit 1
          fi
      - uses: docker/build-push-action@v6
        with:
          context: ${{ steps.dockerfile.outputs.context }}
          file: ${{ steps.dockerfile.outputs.path }}
          push: true
          tags: |
            ${{ secrets.ACR_NAME }}.azurecr.io/${{ github.repository }}:${{ matrix.component }}-latest
            ${{ secrets.ACR_NAME }}.azurecr.io/${{ github.repository }}:${{ matrix.component }}-${{ github.sha }}
